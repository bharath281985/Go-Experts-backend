# Database Directory

This directory is intended for:
- Database schema dumps (e.g., `.sql` files)
- ER Diagrams
- Additional database migration scripts or backups

Currently, the primary schema definition is maintained in `../backend/prisma/schema.prisma`.
