const { PrismaClient } = require(" @prisma/client\); const prisma = new PrismaClient(); async function main() { const users = await prisma.user.findMany({ select: { id: true, role: true, email: true } }); console.log(\Total users:\, users.length); console.log(\Roles:\, [...new Set(users.map(u => u.role))]); console.log(\Users:\, users.slice(0, 10)); } main().catch(console.error).finally(() => prisma.());
